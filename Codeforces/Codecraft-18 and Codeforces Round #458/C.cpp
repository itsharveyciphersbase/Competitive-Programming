#include<bits/stdc++.h>
using namespace std;

string input;
int k;
int op[1001];
vector<int> v[5];

int main(void){
  // for(int i = 2; i < 1001; ++i){
  //   op[i] = op[__builtin_popcount (i)] + 1;
  //   v[op[i]].push_back(i);
  // }
  // cin >> input >> k;
  // if(k > 5) puts("0");
  // else {
  //   int ln = input.size();
  //
  // }
  cout << sqrt(0);
  // cout << go(0, 0);
}
