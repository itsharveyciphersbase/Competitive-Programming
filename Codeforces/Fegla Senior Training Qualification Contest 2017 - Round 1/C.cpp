#include <bits/stdc++.h>

using namespace std;

int main(void) {
    freopen("paint.in", "r", stdin);
    int test, n;
    scanf("%d", &test);
    while (test--) {
        scanf("%d", &n);
        printf("%d\n", n * 2);
    }
}
